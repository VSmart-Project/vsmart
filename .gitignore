# OS
.DS_Store

# Secrets / environment files
.env
.env.*
!.env.example

# Firmware device credentials (AWS IoT Core)
certs/*.key
certs/*.pem
certs/*.crt
!certs/AmazonRootCA*.pem
include/secrets.h

# Dependencies (reinstall locally instead of committing)
node_modules/
.venv/
venv/
**/ios/Pods/
**/android/.gradle/
**/android/build/
**/.gradle/

# Build output / local caches
.pio/
.cache/
.expo/
dist/
build/
*.pyc
__pycache__/
.vscode/
.mcp-backups/

# OSRM map data (large binaries, regenerate via prepare-data.sh)
vsmart-osrm/data/

# Reports & auto-generated hardware snapshots
REPORT.pdf
vsmart-firmware/reports/
vsmart-hardware/reports/
vsmart-hardware/snapshots/
vsmart-module-esp32/reports/
vsmart-module-esp32/snapshots/

# Ad-hoc debug/fix scratch sessions
pcb-debug-2026-09-09/
pcb-fix-2026-09-09/

# Local Claude Code / agent tooling, ephemeral runtime state
.agents/
.claude/
.runtime/
.impeccable/

# Vendored third-party reference material (not part of the app, see CLAUDE.md)
ESP32_OSM_NAV/
guidance-for-tracking-assets-and-locating-devices-using-aws-iot/
